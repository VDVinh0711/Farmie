using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UI_ChoseRoom : MonoBehaviour
{
    /*[SerializeField] private RoomSever _roomSever;
    [SerializeField] private Transform _buttonchosePre;
    [SerializeField] private Transform _rootSpawn;

    private void Start()
    {
        LoadRoomUiChose();
    }

    private void LoadRoomUiChose()
    {
        RefeshObj();
        foreach (var roomValue in _roomSever.ListRoom.Values)
        {
            Instantiate(_buttonchosePre);
        }
    }

    private void RefeshObj()
    {
        foreach (var obj in _rootSpawn.transform.GetComponentsInChildren<RectTransform>())
        {
             Destroy(obj.gameObject);
        }
    }*/
    
    
}
